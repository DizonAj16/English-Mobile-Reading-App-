import 'package:flutter/material.dart';

class StoryListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Story List Page', style: Theme.of(context).textTheme.titleLarge),
    );
  }
}
